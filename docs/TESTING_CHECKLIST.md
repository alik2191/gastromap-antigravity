# 🧪 Чек-лист Тестирования Admin.jsx

**Дата**: 21 января 2026  
**Версия**: После рефакторинга Этапов 1-2  
**Коммит**: `794868e`

---

## ✅ Предварительная Проверка

### Компиляция
- [ ] `npm run build` - успешно
- [ ] Нет ошибок в консоли браузера при загрузке
- [ ] Все импорты корректны

### Dev Server
- [ ] `npm run dev` запускается
- [ ] Hot reload работает
- [ ] Нет ошибок в терминале

---

## 📋 Основной Функционал

### 1. Загрузка Данных (useAdminData)

**Проверить**:
- [ ] Админ панель загружается без ошибок
- [ ] Отображаются все вкладки
- [ ] Данные загружаются корректно:
  - [ ] Локации
  - [ ] Пользователи
  - [ ] Подписки
  - [ ] Отзывы (Feedback)
  - [ ] Отзывы (Reviews)
  - [ ] AI Агенты

**Ожидаемое поведение**:
- Показывается индикатор загрузки
- После загрузки данные отображаются в таблицах
- Нет ошибок в консоли

---

### 2. CRUD Операции с Локациями (useAdminMutations)

#### 2.1 Создание Локации

**Шаги**:
1. Нажать кнопку "Добавить локацию"
2. Заполнить форму:
   - Название
   - Адрес
   - Координаты
   - Описание
3. Нажать "Сохранить"

**Проверить**:
- [ ] Форма открывается
- [ ] Все поля доступны для ввода
- [ ] Валидация работает (обязательные поля)
- [ ] После сохранения:
  - [ ] Показывается toast "Локация успешно создана"
  - [ ] Форма закрывается
  - [ ] Список локаций обновляется
  - [ ] Новая локация появляется в списке

**Ожидаемые вызовы**:
- `handleSaveLocation()` → `createLocation.mutateAsync()`
- `refetchAll()` обновляет данные

#### 2.2 Редактирование Локации

**Шаги**:
1. Найти локацию в списке
2. Нажать кнопку редактирования (карандаш)
3. Изменить данные
4. Нажать "Сохранить"

**Проверить**:
- [ ] Форма открывается с заполненными данными
- [ ] Можно изменить любое поле
- [ ] После сохранения:
  - [ ] Показывается toast "Локация обновлена"
  - [ ] Форма закрывается
  - [ ] Изменения отображаются в списке

**Ожидаемые вызовы**:
- `handleSaveLocation()` → `updateLocation.mutateAsync()`
- `refetchAll()` обновляет данные

#### 2.3 Публикация Локации

**Шаги**:
1. Найти локацию со статусом "pending"
2. Нажать кнопку "Одобрить"

**Проверить**:
- [ ] Показывается toast "Локация опубликована"
- [ ] Статус меняется на "published"
- [ ] Локация перемещается в соответствующий раздел

**Ожидаемые вызовы**:
- `handlePublishLocation()` → `updateLocation.mutateAsync({ status: 'published' })`

#### 2.4 Отклонение Локации

**Шаги**:
1. Найти локацию со статусом "pending"
2. Нажать кнопку "Отклонить"

**Проверить**:
- [ ] Показывается toast "Локация отклонена"
- [ ] Статус меняется на "rejected"

**Ожидаемые вызовы**:
- `handleRejectLocation()` → `updateLocation.mutateAsync({ status: 'rejected' })`

#### 2.5 Удаление Локации

**Шаги**:
1. Найти локацию
2. Нажать кнопку удаления (корзина)
3. Подтвердить удаление

**Проверить**:
- [ ] Показывается диалог подтверждения
- [ ] После подтверждения:
  - [ ] Показывается toast "Локация удалена"
  - [ ] Локация исчезает из списка

**Ожидаемые вызовы**:
- `handleDeleteLocation()` → `deleteLocation.mutateAsync()`

---

### 3. Операции с Подписками

**Шаги**:
1. Перейти на вкладку "Подписки"
2. Изменить статус подписки через Select

**Проверить**:
- [ ] Select открывается
- [ ] Можно выбрать статус (active, expired, cancelled)
- [ ] После изменения:
  - [ ] Показывается toast "Статус обновлён"
  - [ ] Статус обновляется в таблице

**Ожидаемые вызовы**:
- `updateSubscription.mutate({ id, data: { status } })`

---

### 4. Операции с Отзывами (Reviews)

**Шаги**:
1. Перейти на вкладку "Отзывы"
2. Попробовать:
   - Одобрить отзыв
   - Отклонить отзыв
   - Скрыть/показать отзыв

**Проверить**:
- [ ] Кнопки "Одобрить" и "Отклонить" работают
- [ ] Показываются соответствующие toast
- [ ] Статус обновляется
- [ ] Кнопка "Скрыть/Показать" работает

**Ожидаемые вызовы**:
- `updateReviewStatus.mutate({ id, status: 'approved' })`
- `updateReviewStatus.mutate({ id, status: 'rejected' })`
- `api.entities.Review.update()` для is_hidden

---

### 5. Обработка Ошибок

**Проверить**:
- [ ] При ошибке сети показывается понятное сообщение
- [ ] Ошибки логируются в консоль
- [ ] Toast с ошибкой отображается
- [ ] Приложение не падает

**Тестовые сценарии**:
1. Отключить интернет и попробовать создать локацию
2. Попробовать создать дубликат
3. Попробовать удалить несуществующую запись

---

### 6. Loading States

**Проверить**:
- [ ] При создании локации кнопка показывает Loader
- [ ] При обновлении данных показывается индикатор
- [ ] Кнопки блокируются во время операции
- [ ] После завершения кнопки разблокируются

---

### 7. Рефреш Данных

**Проверить**:
- [ ] После любой мутации данные обновляются
- [ ] `refetchAll()` вызывается корректно
- [ ] Нет дублирования запросов
- [ ] Кеш инвалидируется правильно

---

## 🐛 Известные Проблемы

### Исправленные
- ✅ `useMutation is not defined` - добавлен импорт
- ✅ `locationMutation is not defined` - заменено на handleSaveLocation
- ✅ `moderation_rounds table not found` - удален запрос

### Требуют Внимания
- ⚠️ `Upload is not defined` - возможно проблема с кешем браузера
- ⚠️ `updateRegionStatusMutation` - осталась старая мутация (сложная логика)

---

## 🔧 Действия При Ошибках

### Если ошибка "Upload is not defined"
1. Остановить dev server (Ctrl+C)
2. Очистить кеш: `rm -rf node_modules/.vite`
3. Перезапустить: `npm run dev`
4. Жесткая перезагрузка браузера: Ctrl+Shift+R

### Если данные не загружаются
1. Проверить консоль браузера
2. Проверить Network tab
3. Убедиться что Supabase доступен
4. Проверить токен авторизации

### Если мутации не работают
1. Проверить консоль на ошибки
2. Убедиться что useAdminMutations импортирован
3. Проверить что методы вызываются корректно
4. Проверить Network tab для API запросов

---

## 📊 Метрики Производительности

**Проверить**:
- [ ] Время загрузки админ панели < 2 сек
- [ ] Время отклика на действия < 500 мс
- [ ] Нет утечек памяти
- [ ] Нет лишних ре-рендеров

---

## ✅ Финальная Проверка

### Перед Деплоем
- [ ] Все тесты из чек-листа пройдены
- [ ] Нет ошибок в консоли
- [ ] Нет предупреждений в консоли
- [ ] `npm run build` успешно
- [ ] Все изменения закоммичены в Git
- [ ] Создан PR с описанием изменений

### После Деплоя
- [ ] Проверить на production
- [ ] Убедиться что данные загружаются
- [ ] Протестировать основные операции
- [ ] Проверить логи на ошибки

---

## 📝 Отчет о Тестировании

**Заполнить после тестирования**:

### Пройденные Тесты
- Всего: __/50
- Успешно: __
- Провалено: __

### Найденные Баги
1. 
2. 
3. 

### Рекомендации
1. 
2. 
3. 

---

**Тестировщик**: _________________  
**Дата**: _________________  
**Подпись**: _________________
